# 获取暂存区中的代码文件 格式化和校验后再加入暂存区
file=$(git diff --name-only --cached | grep -E '\.(js|ts|jsx|tsx|vue|json|md|css|scss|less|html)$')
if [ -n "$file" ]; then
  prettier --write $file
  git add $file
  eslint --no-fix $file
fi